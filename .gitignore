node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# Credenciais. Normalmente o token fica FORA do projeto, em
# %USERPROFILE%\.claude-usage-monitor\auth.json — mas o auth.js respeita a
# variavel CLAUDE_CONFIG_DIR, e se ela apontar para ca o arquivo nasce dentro
# do repositorio. Estas linhas garantem que ele nunca seja commitado.
auth.json
**/auth.json
usage-monitor/
.claude-usage-monitor/
.claude-glass/
.env
.env.*
